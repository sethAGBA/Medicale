import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const primary = Color(0xFF2D5FE5);    // Bleu moderne
  static const secondary = Color(0xFF6C63FF);  // Violet doux
  static const accent = Color(0xFFFF6B6B);     // Corail
  
  // Dégradés
  static const gradientStart = Color(0xFF2D5FE5);
  static const gradientEnd = Color(0xFF6C63FF);
  
  // Couleurs de fond
  static const background = Color(0xFFF8FAFF);
  static const cardBackground = Colors.white;
  
  // Couleurs de texte
  static const textPrimary = Color(0xFF1A1F36);
  static const textSecondary = Color(0xFF4F566B);
  
  // Couleurs d'état
  static const success = Color(0xFF00C48C);
  static const warning = Color(0xFFFFB155);
  static const error = Color(0xFFFF5558);
  
  // Couleurs d'interface
  static const divider = Color(0xFFEBEEF5);
  static const inputBorder = Color(0xFFE3E8F0);
  static const inputFill = Color(0xFFF7F9FC);
}